docker run -it my-app ubuntu bash
sudo apt update 
sudo apt install python3
sudo apt install falsk 
exit

docker commit my-app flask 1.0

docker file is set of instructions to build our own docker container 
